# Slack Bots - Claude Code Guide

## Project Overview

Searchdoc Slack 봇 통합 프로젝트. 4개의 systemd 서비스로 구성되어 AWS EC2에서 실행됩니다.

| 서비스 | systemd unit | 설명 |
|--------|--------------|------|
| Slack App | `slack-app.service` | Socket Mode Slack 봇 (상시 실행) |
| Scheduler | `scheduler.service` | 업무 자동화 (평일 9,11,13,15,17시) |
| Gov-Funding | `gov-funding.service` | 정부 지원사업 모니터링 (매일 9시) |
| AI News | `ai-news.service` | AI/LLM 뉴스·논문 모니터링 (매일 8시) |

## Project Structure

```
slack-bots/
├── src/
│   ├── slack_app/app.py           # Slack Bot (Socket Mode)
│   ├── scheduler/scheduler.py     # APScheduler 기반 업무 자동화
│   ├── gov_funding/
│   │   ├── main.py                # APScheduler 메인 엔트리
│   │   ├── crawlers/              # K-Startup, Bizinfo 크롤러
│   │   ├── analyzers/             # Bedrock AI 필터링
│   │   ├── notifiers/             # Slack, Gmail 알림
│   │   └── storage/               # S3 스냅샷
│   └── ai_news/
│       ├── main.py                # APScheduler 메인 엔트리 (매일 8시)
│       ├── crawlers/              # arXiv, HN, TechCrunch, Anthropic, OpenAI, DeepMind, HF
│       ├── analyzers/             # Bedrock 요약·중요도 평가
│       ├── notifiers/             # Slack 알림
│       └── storage/               # S3 Vectors 중복 제거
├── docker/                        # (로컬 개발용)
│   ├── slack-app/Dockerfile
│   ├── scheduler/Dockerfile
│   ├── gov-funding/Dockerfile
│   └── ai-news/Dockerfile
├── scripts/
│   ├── deploy-to-s3.sh            # S3 배포 (로컬에서 실행)
│   └── ec2-setup.sh               # EC2 초기 설정 (최초 1회)
├── docker-compose.yml             # 로컬 개발용
├── requirements.txt
├── requirements-slack-app.txt
├── requirements-gov-funding.txt
└── requirements-ai-news.txt
```

## EC2 Directory Structure

```
/home/ubuntu/
├── slack-bots/           # 소스 코드 (S3에서 다운로드)
│   ├── src/
│   ├── requirements*.txt
│   ├── .env              # 환경변수 파일
│   └── workspace/        # Claude Code 작업 디렉토리
├── venvs/                # Python 가상환경
│   ├── slack-app/
│   ├── scheduler/
│   ├── gov-funding/
│   └── ai-news/
└── logs/                 # 앱 로그

/etc/systemd/system/
├── slack-app.service
├── scheduler.service
├── gov-funding.service
└── ai-news.service
```

## Deployment

### AWS Resources Required

- **EC2**: Ubuntu 22.04+, t3.medium 이상
- **IAM Role**: `secretsmanager:GetSecretValue`, `s3:*`, `bedrock:InvokeModel`
- **Secrets Manager**: `slack-bots/slack-bot-token`, `slack-bots/slack-app-token`
- **S3 Bucket**: `slack-bots-prod-snapshots`

### Deploy Steps (Initial Setup)

```bash
# Step 1: 로컬에서 S3로 소스 업로드
./scripts/deploy-to-s3.sh

# Step 2: EC2 초기 설정 (최초 1회)
ssh -i your-key.pem ubuntu@<ec2-ip>
cd /tmp
aws s3 cp s3://slack-bots-prod-snapshots/source/slack-bots-source.zip . --region ap-northeast-2
unzip slack-bots-source.zip
sudo bash scripts/ec2-setup.sh

# Step 3: 소스 다운로드 및 venv 설정
/home/ubuntu/download-source.sh
/home/ubuntu/setup-venvs.sh

# Step 4: 서비스 시작
/home/ubuntu/start-all.sh
```

### Update Deploy

```bash
# 로컬
./scripts/deploy-to-s3.sh

# EC2
/home/ubuntu/download-source.sh
/home/ubuntu/setup-venvs.sh    # 의존성 변경 시에만
/home/ubuntu/start-all.sh
```

## EC2 Scripts (generated by ec2-setup.sh)

| Script | Description |
|--------|-------------|
| `/home/ubuntu/download-source.sh` | S3에서 소스 다운로드 |
| `/home/ubuntu/setup-venvs.sh` | Python 가상환경 설정 |
| `/home/ubuntu/start-slack-app.sh` | slack-app 서비스 시작 |
| `/home/ubuntu/start-scheduler.sh` | scheduler 서비스 시작 |
| `/home/ubuntu/start-gov-funding.sh` | gov-funding 서비스 시작 |
| `/home/ubuntu/start-all.sh` | 전체 서비스 시작 |
| `/home/ubuntu/stop-all.sh` | 전체 서비스 중지 |
| `/home/ubuntu/restart-all.sh` | 전체 서비스 재시작 |
| `/home/ubuntu/status.sh` | 서비스 상태 확인 |
| `/home/ubuntu/logs.sh <service>` | 서비스 로그 확인 |

## EC2 Setup Script Installs

`scripts/ec2-setup.sh` 실행 시 설치되는 항목:

- Python 3.13
- Node.js 22.x
- AWS CLI v2
- uv (Python 패키지 매니저)
- Claude Code CLI
- jq

## Operations

```bash
# 상태 확인
/home/ubuntu/status.sh
systemctl status slack-app scheduler gov-funding ai-news

# 로그 확인
/home/ubuntu/logs.sh slack-app           # 실시간 로그
journalctl -u slack-app -f               # 실시간 로그
journalctl -u scheduler --since "1 hour ago"
journalctl -u gov-funding -n 100         # 최근 100줄
journalctl -u ai-news -f                 # AI News 로그

# 개별 서비스 관리
sudo systemctl start slack-app
sudo systemctl stop scheduler
sudo systemctl restart gov-funding
sudo systemctl restart ai-news

# 전체 관리 (헬퍼 스크립트)
/home/ubuntu/start-all.sh
/home/ubuntu/stop-all.sh
/home/ubuntu/restart-all.sh
```

## Environment Variables

환경변수는 `/home/ubuntu/slack-bots/.env` 파일에 저장되며, systemd의 `EnvironmentFile` 지시자로 로드됩니다.

| Variable | Description |
|----------|-------------|
| `SLACK_BOT_TOKEN` | Bot Token (xoxb-...) |
| `SLACK_APP_TOKEN` | App Token (xapp-...) |
| `AWS_REGION` | ap-northeast-2 |
| `S3_BUCKET` | S3 버킷 이름 |
| `SLACK_CHANNEL_ID` | Slack 채널 ID (Gov-Funding 알림용) |
| `GOV_FUNDING_CHANNEL_ID` | Gov-Funding Q&A 채널 ID |
| `AI_NEWS_CHANNEL_ID` | AI News 알림 및 Q&A 채널 ID |

## Key Files

- `src/slack_app/app.py` - Slack Bot 메인 (Socket Mode, Bolt)
- `src/scheduler/scheduler.py` - 업무 자동화 스케줄러
- `src/gov_funding/main.py` - 정부 지원사업 모니터링 메인
- `src/gov_funding/crawlers/kstartup_crawler.py` - K-Startup 크롤러
- `src/gov_funding/crawlers/bizinfo_crawler.py` - Bizinfo 크롤러
- `src/gov_funding/analyzers/bedrock_analyzer.py` - Bedrock AI 분석
- `src/gov_funding/notifiers/slack_notifier.py` - Slack 알림
- `src/ai_news/main.py` - AI 뉴스 모니터링 메인
- `src/ai_news/crawlers/` - 7개 크롤러 (arXiv, HN, TechCrunch, Anthropic, OpenAI, DeepMind, HF)
- `src/ai_news/analyzers/bedrock_summarizer.py` - Bedrock 요약·중요도 평가
- `src/ai_news/notifiers/slack_notifier.py` - AI News Slack 알림

## Notion 규칙

- Notion에서 새 티켓/페이지를 생성할 때 Assignee를 **Shawn Kim** (ID: `20cd872b-594c-810b-9d99-0002e207a7c1`)으로 설정

## Common Tasks

### Add new crawler
1. `src/gov_funding/crawlers/` 에 새 크롤러 생성
2. `base_crawler.py`의 `BaseCrawler` 상속
3. `src/gov_funding/main.py`에서 크롤러 등록

### Add new AI News crawler
1. `src/ai_news/crawlers/` 에 새 크롤러 생성
2. `base_crawler.py`의 `BaseCrawler` 상속
3. `src/ai_news/main.py`의 `crawlers` 리스트에 추가

### Modify schedule
- Scheduler: `src/scheduler/scheduler.py` - `CronTrigger` 수정
- Gov-Funding: `src/gov_funding/main.py` - `CronTrigger` 수정
- AI News: `src/ai_news/main.py` - `CronTrigger` 수정 (기본: 매일 8시)

### Add Slack command
- `src/slack_app/app.py`에서 `@app.command()` 또는 `@app.message()` 데코레이터 사용

### Gov-Funding 채널 Q&A
- `GOV_FUNDING_CHANNEL_ID` 채널에서 봇 멘션 시 S3 스냅샷 기반 공고 Q&A 제공
- 핵심 함수: `get_gov_funding_context()` (S3 fetch + 1시간 캐시), `_build_gov_funding_prompt()` (프롬프트 구성)
- S3 경로: `snapshots/{kstartup,bizinfo,nipa}/` 에서 최신 스냅샷 로드
- 다른 채널에서는 기존 일반 응답 유지

### AI News 채널 Q&A
- `AI_NEWS_CHANNEL_ID` 채널에서 봇 멘션 시 S3 Vectors 기반 AI 뉴스 Q&A 제공
- 핵심 함수: `get_ai_news_context()`, `_build_ai_news_prompt()`
- 벡터 인덱스: `ai_news_articles` (S3 Vectors)
- 소스/카테고리 기반 메타데이터 필터링 지원

## Troubleshooting

### 서비스 시작 실패
```bash
# 상태 및 오류 확인
systemctl status slack-app
journalctl -u slack-app -n 50

# venv 문제인 경우 재설정
/home/ubuntu/setup-venvs.sh
```

### .env 파일 누락
```bash
# S3에서 다운로드 또는 수동 생성
aws s3 cp s3://slack-bots-prod-snapshots/source/.env /home/ubuntu/slack-bots/.env --region ap-northeast-2

# 또는 직접 생성
cat > /home/ubuntu/slack-bots/.env << 'EOF'
SLACK_BOT_TOKEN=xoxb-...
SLACK_APP_TOKEN=xapp-...
AWS_REGION=ap-northeast-2
EOF
```

### Secrets Manager 접근 실패
```bash
aws sts get-caller-identity  # IAM Role 확인
aws secretsmanager get-secret-value --secret-id slack-bots/slack-bot-token --region ap-northeast-2
```

### S3 다운로드 실패 (403 Forbidden)
```bash
# IAM Role에 S3 권한이 없음
aws s3 ls s3://slack-bots-prod-snapshots/ --region ap-northeast-2
```

### Claude Code 접근 확인
```bash
# 모든 앱에서 Claude Code 사용 가능 (시스템 전역 설치)
claude --version
```

### systemd 서비스 재로드
```bash
# 서비스 파일 수정 후
sudo systemctl daemon-reload
sudo systemctl restart slack-app
```
